<h2>Add New Category</h2>

<% if @category.errors.any? %>
  <ul class="errors">
    <% @category.errors.each do |column, message| %>
    <li>
      <strong>
        <%= column.capitalize %>
      </strong> <%= message %>
    </li>
    <% end %>
  </ul>
<% end %>
<%= render partial: 'form' %>
