
  <%= form_for @product do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <%= f.label :photo_url %>
    <%= f.text_field :photo_url %>
    <%= f.label :inventory %>
    <%= f.number_field :inventory %>
    <%= f.radio_button(:retired, false) %>
    <%= f.label "Keep this product active" %>
    <%= f.radio_button(:retired, true) %>
    <%= f.label "Retire this product" %>
    <%= f.submit "Submit", class: "success button" %>
  <% end %>

<%#IDEA, make this method apart of a default within the database or model %>
<%#{:id, default= (1..9999).random}) %>
<%# IDEA, same as for id "User ID" %>
