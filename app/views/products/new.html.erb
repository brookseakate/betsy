
  <%= form_for @product do |f| %>
  <%= label "Name" %>
  <%= text_field :name %>
  <%= label "Description" %>
  <%= text_field :description %>
  <%= label "photo url" %>
  <%= text_field :photo_url %>
  <%= label "inventory" %>
  <%= text_field :inventory %>
  <%= label  "Is this product available currently?" %>
  <%= radio_button("product", "retired", "Yes.")%>
  <%= radio_button("product", "retired", "No, retire this product.")

<% end %>

<%#IDEA, make this method apart of a default within the database or model %>
<%#{:id, default= (1..9999).random}) %>
<%# IDEA, same as for id "User ID" %>
